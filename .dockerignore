# Keep the build context small and the image reproducible. Anything generated
# on a developer's machine is rebuilt inside the image instead.
.git/
.github/
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

frontend/node_modules/
frontend/.next/
frontend/out/
frontend/tsconfig.tsbuildinfo
frontend/screenshots/

# Fetched during the build, under its own licence.
src/bhashasetu/language_packs/bn/data/hunspell/

bhashasetu.db*
tests/
docs/
docker/
docker-compose.yml
